# 🎬 Subtitle Translator Pro

## برنامج ترجمة احترافي للأفلام مدعوم بالذكاء الاصطناعي

### 🌟 المميزات الرئيسية

#### 1. **ترجمة فورية وسريعة**
- ترجمة فورية مثل Google Translate Web (أقل من ثانية لكل سطر)
- معالجة متوازية للملفات المتعددة
- لا حاجة للانتظار - بدء الترجمة فوراً

#### 2. **دعم خدمات ترجمة متعددة**
- **Google Translate** (مجاني): سريع جداً وفوري
- **Gemini AI** (مدفوع): ذكي مع فهم السياق
- **DeepL API** (مدفوع): احترافي وعالي الجودة
- **Yandex Translate** (مدفوع): خيار إضافي
- إمكانية إضافة خدمات جديدة بسهولة

#### 3. **ترجمة جماعية (Bulk Translation)**
- ترجمة حتى 20 ملف دفعة واحدة
- حد أقصى 1 ميجابايت لكل ملف
- إدارة ذكية لحدود الطلبات (Rate Limiting)
- معالجة متوازية مع احترام حدود API

#### 4. **حفظ السياق (Context Preservation)**
- نظام ذكي لحفظ سياق الحوار بين الجمل
- نافذة سياق قابلة للتخصيص (3-5 جمل سابقة)
- متاح مع خدمة Gemini AI فقط
- يحسن دقة الترجمة بشكل كبير

#### 5. **دعم صيغ متعددة**
- **SRT** (SubRip)
- **VTT** (WebVTT)
- **ASS** (Advanced SubStation Alpha)
- **SUB** (SubViewer)
- **SBV** (YouTube Subtitles)
- **STL** (Spruce Subtitle File)

#### 6. **محرر ترجمات متقدم**
- تعديل النصوص المترجمة
- تعديل التوقيتات (وقت البداية والنهاية)
- واجهة جدولية سهلة الاستخدام
- حفظ فوري للتعديلات

#### 7. **إدارة الملفات**
- تحميل جميع الملفات كملف ZIP واحد
- حذف المشاريع القديمة
- معاينة الملفات قبل الترجمة
- تتبع حالة كل ملف

---

## 📦 التثبيت والإعداد

### 1. متطلبات النظام
```bash
Python 3.8+
pip (Python Package Manager)
```

### 2. تثبيت المتطلبات
```bash
cd subtitle-translator-pro
pip install -r requirements.txt
```

### 3. إعداد المتغيرات البيئية
```bash
# نسخ ملف الإعدادات النموذجي
cp .env.example .env

# تعديل الملف وإضافة مفاتيح API
nano .env
```

في ملف `.env`:
```env
# API Keys (اختياري - فقط للخدمات المدفوعة)
GEMINI_API_KEY=your_gemini_api_key_here
DEEPL_API_KEY=your_deepl_api_key_here

# إعدادات الخادم
FLASK_ENV=development
PORT=5000

# إعدادات الترجمة
MAX_FILES_PER_BATCH=20
MAX_FILE_SIZE_MB=1
REQUEST_RATE_LIMIT=100
CONTEXT_WINDOW_SIZE=5
USE_CONTEXT_PRESERVATION=True
```

### 4. تشغيل البرنامج
```bash
python app.py
```

سيعمل الخادم على: `http://localhost:5000`

---

## 🚀 دليل الاستخدام

### الطريقة 1: واجهة الويب

#### 1. رفع الملفات
- افتح المتصفح واذهب إلى `http://localhost:5000`
- اسحب الملفات وأفلتها، أو انقر لاختيارها
- يمكنك رفع حتى 20 ملف في وقت واحد

#### 2. اختيار إعدادات الترجمة
- **اللغة المصدر**: اختر اللغة أو اترك "كشف تلقائي"
- **اللغة الهدف**: اختر اللغة المطلوبة (مثلاً: العربية)
- **خدمة الترجمة**: 
  - Google (سريع ومجاني)
  - Gemini (ذكي - يتطلب API Key)
  - DeepL (احترافي - يتطلب API Key)
- **حفظ السياق**: فعّل لترجمة أفضل (مع Gemini فقط)

#### 3. بدء الترجمة
- انقر "بدء الترجمة"
- راقب شريط التقدم
- انتظر حتى الانتهاء

#### 4. تحميل أو تعديل النتائج
- **تحميل الكل**: احصل على ملف ZIP بجميع الترجمات
- **تعديل الترجمات**: افتح المحرر لتعديل أي نص
- **حذف المشروع**: احذف الملفات إذا لم تعد بحاجة إليها

---

### الطريقة 2: API

البرنامج يوفر REST API كامل للاستخدام البرمجي.

#### 1. رفع الملفات
```bash
curl -X POST http://localhost:5000/api/upload \
  -F "files=@subtitle1.srt" \
  -F "files=@subtitle2.srt"
```

**الاستجابة:**
```json
{
  "success": true,
  "job_id": "abc123...",
  "files": [
    {
      "name": "subtitle1.srt",
      "format": "srt",
      "size": 12345
    }
  ]
}
```

#### 2. بدء الترجمة
```bash
curl -X POST http://localhost:5000/api/translate \
  -H "Content-Type: application/json" \
  -d '{
    "job_id": "abc123...",
    "source_lang": "en",
    "target_lang": "ar",
    "service": "google",
    "use_context": false
  }'
```

#### 3. التحقق من الحالة
```bash
curl http://localhost:5000/api/status/abc123...
```

**الاستجابة:**
```json
{
  "success": true,
  "job": {
    "job_id": "abc123...",
    "status": "processing",
    "progress": 45,
    "total_files": 2
  }
}
```

#### 4. تحميل النتائج
```bash
curl -O http://localhost:5000/api/download/abc123...
```

---

## 🔧 ميزات متقدمة

### 1. إضافة خدمة ترجمة مخصصة

يمكنك إضافة خدمة ترجمة جديدة بسهولة:

```python
from utils.translation_engine import TranslationService, TranslationEngine

class MyCustomService(TranslationService):
    def translate(self, text: str, source_lang: str, target_lang: str) -> str:
        # أضف منطق الترجمة الخاص بك هنا
        return translated_text
    
    def is_available(self) -> bool:
        return True

# إضافة الخدمة
engine = TranslationEngine()
engine.add_custom_service('mycustom', MyCustomService())
```

### 2. تخصيص نافذة السياق

في ملف `config.py`:
```python
CONTEXT_WINDOW_SIZE = 5  # عدد الجمل السابقة للسياق
```

### 3. تخصيص حدود الطلبات

```python
REQUEST_RATE_LIMIT = 100  # عدد الطلبات
RATE_LIMIT_WINDOW_SECONDS = 60  # في 60 ثانية
```

---

## 🏗️ البنية المعمارية

```
subtitle-translator-pro/
│
├── app.py                      # تطبيق Flask الرئيسي
├── config.py                   # إعدادات التطبيق
├── requirements.txt            # المكتبات المطلوبة
├── .env.example               # ملف الإعدادات النموذجي
│
├── utils/
│   ├── __init__.py
│   ├── subtitle_parser.py     # محلل صيغ الترجمة
│   └── translation_engine.py  # محرك الترجمة
│
├── templates/
│   └── index.html             # واجهة الويب
│
├── uploads/                   # ملفات مرفوعة (مؤقتة)
└── outputs/                   # ملفات مترجمة
```

---

## 🌐 اللغات المدعومة

البرنامج يدعم أكثر من 80 لغة، منها:

- العربية (ar)
- الإنجليزية (en)
- الإسبانية (es)
- الفرنسية (fr)
- الألمانية (de)
- الإيطالية (it)
- البرتغالية (pt)
- الروسية (ru)
- الصينية (zh)
- اليابانية (ja)
- الكورية (ko)
- التركية (tr)
- الهندية (hi)
- وأكثر...

---

## 📱 تطبيق Android (قريباً)

التطبيق قيد التطوير حالياً وسيتضمن:
- واجهة مستخدم أصلية (Native UI)
- دعم كامل لجميع الميزات
- مزامنة مع الخادم
- العمل دون اتصال (Offline Mode)

---

## ⚡ نصائح للأداء الأمثل

### 1. اختيار خدمة الترجمة المناسبة

| الخدمة | السرعة | الدقة | التكلفة | الاستخدام الموصى به |
|--------|---------|-------|---------|---------------------|
| Google | ⚡⚡⚡ فوري | ⭐⭐⭐ جيد | مجاني | ترجمة سريعة عامة |
| Gemini | ⚡⚡ متوسط | ⭐⭐⭐⭐⭐ ممتاز | مدفوع | أفلام وحوارات معقدة |
| DeepL | ⚡⚡ متوسط | ⭐⭐⭐⭐ ممتاز | مدفوع | نصوص احترافية |

### 2. استخدام حفظ السياق
- فعّل "حفظ السياق" للأفلام والمسلسلات
- يحسن فهم الحوارات المترابطة
- متاح فقط مع Gemini AI

### 3. الترجمة الجماعية
- ارفع جميع حلقات المسلسل دفعة واحدة
- وفّر الوقت والجهد
- معالجة أسرع

### 4. التحقق والتعديل
- راجع الترجمات باستخدام المحرر
- عدّل التوقيتات إذا لزم الأمر
- احفظ التعديلات قبل التحميل

---

## 🔒 الأمان والخصوصية

- جميع الملفات تُحذف تلقائياً بعد 24 ساعة
- لا يتم حفظ مفاتيح API في قواعد البيانات
- الاتصالات مشفرة (استخدم HTTPS في الإنتاج)
- لا يتم مشاركة المحتوى مع أطراف ثالثة

---

## 🐛 حل المشاكل

### المشكلة: "خطأ في رفع الملفات"
**الحل:**
- تأكد أن حجم الملف أقل من 1 ميجابايت
- تحقق من صيغة الملف (يجب أن تكون مدعومة)

### المشكلة: "خدمة الترجمة غير متاحة"
**الحل:**
- تحقق من صحة مفتاح API في ملف `.env`
- تأكد من اتصالك بالإنترنت

### المشكلة: "الترجمة بطيئة"
**الحل:**
- استخدم Google Translate للسرعة
- قلل عدد الملفات في الدفعة الواحدة
- تعطيل "حفظ السياق" إذا لم تكن بحاجة إليه

---

## 📞 الدعم والمساهمة

للإبلاغ عن مشاكل أو اقتراح ميزات جديدة:
- افتح Issue في GitHub
- أرسل Pull Request
- راسلنا عبر البريد الإلكتروني

---

## 📄 الترخيص

هذا البرنامج مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

## 🎉 شكر خاص

- **Google Translate**: للترجمة الفورية المجانية
- **Gemini AI**: لقدرات الذكاء الاصطناعي المتقدمة
- **DeepL**: للترجمة الاحترافية عالية الجودة
- **المجتمع**: لجميع المساهمات والاقتراحات

---

**استمتع بترجمة سريعة واحترافية! 🚀**
